name: web-test
namespace: web-test
type: Deployment
tolerationValues:
  - web

pods:
  - name: web-test
    repo: registry.hub.docker.com/lukaspastva/test-web:0.1.44
    resources: 100
    ports:
      - name: http
        port: 8000
        domains:
          - "test.io"
          - "www.test.io"
        paths:
          - "/"
